// each page will have a similar functionality
// there will be an input field for each column in the database
// english
// turkish
// maybe we should make a reusable input component
// will have an input and a label that can be assigned using props
// this should be navigated from either a home page and/or the navbar

import { Input } from "../Components/Input";

export const CreateSentence = () => {
  <Input label="Create Sentence" />;
};
